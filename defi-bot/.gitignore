__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/

.env
.env.local
*.key
wallet*.json

# Legacy single-file fetch format (personal artifacts, don't commit)
data/transcripts/*.json
data/transcripts/*.md

# Never commit raw video files from `bot learn ingest` (they're huge and we keep keyframes instead)
data/transcripts/*/video.*
data/transcripts/*/*.mp4
data/transcripts/*/*.webm
data/transcripts/*/*.mkv

# Bot runtime state (databases) — keep local
data/*.db
data/*.sqlite

.pytest_cache/
.ruff_cache/
.mypy_cache/

.DS_Store
.vscode/
.idea/
