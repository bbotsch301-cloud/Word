# Required for NextAuth.js — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=

# Required in production — your app's base URL
NEXTAUTH_URL=http://localhost:3000

# Optional: Google OAuth
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=

# Optional: GitHub OAuth
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
